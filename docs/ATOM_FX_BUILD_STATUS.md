# ATOM FX — Build Status & Outstanding Items

**Audited:** 2026-09-10, against the actual source in the repo at commit `5dcdc6a` (not a snapshot
this time — read `WheelUiState.kt`, `WheelMapper.kt`, `PairSheet.kt`, `StatusStrip.kt`,
`WheelScreen.kt`, `MainActivity.kt`, `NotificationHistoryScreen.kt`, `.gitignore`, `build.gradle.kts`
directly). Previous audit (2026-09-02) predated Wheel v2's wing rework, the Simplification Rework
(2026-09-05/06/09), the Notification History screen, the COT/Conviction overlay, the CSM dispersion
reliability gate, the Rule #1 currency-strength/regime methodology fix, and the Bollinger Band D1
touch alert + Watchlist screen — all folded in below.
**Correction, 2026-09-10 (same day, caught by Pieter):** the first pass of this refresh still
missed the BB/Watchlist feature (`afd9a25`, 2026-09-09) entirely — it doesn't touch any of the
files this refresh's own grep/read list covered, and it landed one commit before the session this
list's source memory was compiled from, so neither the code search nor the memory caught it.
Lesson: a doc-sync audit needs a `git log --stat` pass over the actual commit range since the last
audit, not just targeted greps for symbols already known to exist.
This is the **living tracker** the project was missing — the specs describe the whole destination;
this maps how far it's built. Update the Status column as work lands.

**Note (2026-09-17):** the banner above still says "Audited 2026-09-10" even though this file has
been edited piecemeal at least a dozen times since (rows dated 2026-09-16/17 throughout, plus a
full-system 4-area audit landing the same day as this note) — understating its own currency, the
exact pattern this file's own 2026-09-10 lesson above warns about. Not rewriting the historical
banner (it's a real record of that audit's own scope and lesson); treat individual row dates as
the actual source of truth for what's current, not the top banner's single date.

> Nothing in the vision was lost. Every surface below is specified in
> `docs/ATOM_FX_FUNCTIONAL_SPEC.md`, `docs/ATOM_FX_ARCHITECTURE.md`, `docs/ATOM_FX_DESIGN.md`,
> and shown in `docs/mockups/atom-fx-screen-kit.html`. The "thin documentation" feeling was the
> absence of *this* file, not missing specs.

Legend: ✅ done · 🟡 partial · ⬜ not started · 🅿️ post-v1 (deferred on purpose)

---

## A. Backend (Python scanner) — essentially complete

| Item | Status | Where / notes |
|---|---|---|
| Frozen engine + Rule #1 guard (Phase 0) | ✅ | `scanner/*.py`, `tests/`, `rule1.yml` green |
| EXTEND Phase 1 (csm_delta, currency_flow, breadth, structure, potential, spark, schema_version) | ✅ | `scanner/extend/`, wired in `scan_h1.py`, **live in `signals.json`** |
| Macro archetype engine (Phase 1b) | ✅ | `scanner/extend/macro_regime.py`, `macro_regime` live |
| Push transport + firing hook (Phase 6 backend) | ✅ | `push/send_push.py` + `scan_h1.py` `send_push_alert` / `send_push_level_alert` wired; `test_push.yml` green |
| AI recommendation — deterministic seed | ✅ | `recommendation.py` (use_model=False in `scan_h1`); `recommendation` live |
| **AI recommendation — Sonnet narration + cadence (Phase 7 full)** | ✅ | Wired into `scan_news.py`, `use_model=True`. Cadence is a union of three triggers (Pieter's call, 2026-09-03): a qualifying gold signal newer than the last narration, the regime `bias` flipping, or 12h+ since the last narration — whichever comes first. `recommendation.py` now preserves the AI-narrated text across `scan_h1.py`'s hourly seed-only refresh (was previously clobbered every hour regardless of scan_news's cadence — a real bug, fixed alongside this). **Bugfix (2026-09-17, found in a full-system audit):** the bias-flip trigger was comparing `signals.recommendation.bias` against itself — that field gets resynced to the current seed on every hourly `use_model=False` call regardless of whether a narration happened, so the comparison was always "now vs now" and could never fire outside a rare EXTEND-crash edge case. New `recommendation.narrated_bias` field only moves when a narration actually lands (real model text, or a `seed_unchanged` carry-forward), giving `scan_news.py` a baseline that actually reflects "what was last said," not "what's currently true." 1 new regression test, 75/75 extend suite green |
| **`deep_analysis` daily brief 404 fix** | ✅ | `scan_news.py` `SONNET_MODEL` updated from the retired `claude-sonnet-4-20250514` to `claude-sonnet-5`. Also fixed a separate, previously-silent bug: `recommendation.py`'s `_call_sonnet` was calling `scan_news._sonnet(prompt)` with only one of its two required positional args, so every `use_model=True` call was silently raising and falling back to template text |
| **`news_themes` tagging** (headlines → macro axis) | ✅ | `scan_news.py` `tag_theme()` — deterministic keyword match to the same five `macro_regime.py` evidence axes (risk/rates/usd/commodity/safe_haven), no AI call. `breaking.themes` (parallel array to `breaking.headlines`) is new; `recommendation.py`'s prompt now cites the axis per headline so the narration can say things like "rates-axis news confirms…". **Correction 2026-09-10**: this file previously said it never feeds back into `macro_regime`'s own axis scoring, "colour only" — that gap was closed the same day, see the Macro Archetype rework row below |
| **COT-based Conviction overlay** (Signals Roadmap Phase 3) | ✅ | `scanner/extend/conviction.py` — Conviction Extreme alert, adaptive threshold (80 normal / 40 when a currency's COT is stale) so it stays reachable under degraded evidence; `cot_confirmed`/"technical only" messaging flows into the real push |
| **CSM dispersion reliability gate** | ✅ | `scanner/extend/csm_dispersion.py` — caps CSM-derived votes/components to "unreliable" when a TF's dispersion ranks in the bottom 20% of its own rolling ~60-scan history. Needs ~2.5 days of live hourly scans to have accumulated enough history to ever actually trigger — worth checking back once it has |
| **EXTEND layer full audit** | ✅ | All six modules read line-by-line (`csm_delta`, `breadth`, `macro_regime`, `conviction`, `potential`, `recommendation`); one doc-comment fix, two new cross-validation regression tests added, one real threshold bug found and fixed (Conviction Extreme's unreachable-under-COT-staleness ceiling, see above) |
| **Rule #1 sign-off: currency-strength/regime FX methodology** | ✅ | `csm.py` `STRENGTH_PAIRS` 16→18 (added EURJPY/GBPJPY — pure data-completeness, zero new API cost); `regime.py` `classify_regime` Vote 3 rebuilt to measure the same narrow AUD+NZD+CAD-vs-havens concept Vote 1 already uses (was inconsistently mixing in GBP/EUR), Vote 2 gained CAD. Pieter's explicit sign-off after a full FX-methodology discussion, test/golden protocol followed exactly (`test_rule1_frozen` failed as expected, golden diffed and regenerated, `test_extend`'s appearance counts updated) — both suites green |
| **Bollinger Band D1 touch detection** (Signals Roadmap §5) | ✅ | `scanner/extend/bb_touch.py` — 12-period SMA ±2σ on D1 closes (12-period per Pieter's own spec, not the original 20-period port default), checked every hourly scan against the current still-forming D1 candle, plus a band-width trend read (expanding/converging/flat vs ~5 D1 bars back). New `pairs.<PAIR>.bb_d1` key. No auto-scoring or confirmation gate by design — Pieter's own redesign of the original Phase 4 plan, "a wick touch IS the whole signal," judging a real reversal is a manual Watchlist review, not something this module scores. New edge-triggered `state_alerts.py` detector, type `bb_touch`. 6 new tests, 43/43 extend suite green |
| **Rotation / Pulse / Thrust** (concept indicators, from a research + live-mockup pass) | ✅ backend, 🚫 no UI | `scanner/extend/rotation.py` (new), `scanner/extend/market_pulse.py` (new), `breadth.py::compute_thrust` (new fn) — three market-state reads, all pure aggregation of already-computed values (`csm`, `csm_delta`, `breadth`, `macro_regime`, `pairs.<PAIR>.bb_d1`), no new trading math. New `rotation`/`pulse`/`breadth_thrust` keys, `schema_version` 1→2. 9 new tests, 52/52 extend suite green. **Same-day (2026-09-10) UI retired** along with the "Whole Market Indicators" card (see row below) — backend still computes/publishes all three, nothing in the app reads them any more |
| **%B / Board %B** (Pieter's own indicator idea, per-pair + market-wide) | ✅ backend, 🚫 no UI for Board %B | `scanner/extend/bb_touch.py` extended — `compute_bb_d1` now also returns `pctb`/`pctb_sma` (the %B oscillator line + its 12-period signal line, oldest-first, up to 56 D1 bars, not clamped — a real close outside the bands legitimately pierces past 0/100), and a new `compute_board_percent_b` pointwise-averages every pair's line across the board. New `pairs.<PAIR>.bb_d1.pctb`/`.pctb_sma` fields + top-level `percent_b_board` key, `schema_version` 2→3. Needs no scan-to-scan history buffer (D1 closes are already fully available each scan). 6 new tests, 58/58 extend suite green. Per-pair %B is very much alive (ChartSheet, below); **Board %B's own UI retired same day** (2026-09-10) — see "Whole Market Indicators" row |
| **%B chart dates** (Pieter's follow-up ask) | ✅ | New `_d1_dates()` in `bb_touch.py` — `scanner/aggregator.py` (frozen) discards D1 bar dates by design (integer-indexed output); this independently mirrors its exact resample parameters against `scan_h1.py`'s own pre-aggregation `raw_ohlcv` to recover them, verified row-count-aligned against the frozen output via synthetic data before trusting it. New `pairs.<PAIR>.bb_d1.pctb_dates` + `percent_b_board.dates` fields (empty rather than guessed if the alignment check fails), `schema_version` 3→4. `PercentBOscillator.kt` prints 3 sparse date labels (oldest/middle/newest) below the plot when present. Same pass: %B signal line thinned from 2dp to 1dp (was "bolder", now matches the raw line), and the card's own heading demoted from `Title` to the same `Caption` style `InsightsScreen.kt`'s other section headers use. 4 new tests, 61/61 extend suite green |
| **Crowd score series (`crowd_series`, schema v12, Roadmap §5c)** | ✅ backend (on `main`, 2026-09-19) | `scanner/extend/crowd_score.py` + `crowd_data.py` — the Crowded Market indicator as a per-pair D1 + H4 top/bottom score series (regime filter not ported); D1 on the 17:00-NY close, H4 on TradingView's NY-session blocks, market-closed bars dropped, completed bars only; Legacy futures-only COT (`data/cot_legacy/legacy_nc.csv`, weekly refresh in `scan_cot.py` — a different report from the TFF one Conviction uses). Golden-pinned to the research branch's tested port; 27 tests; on real history a 5000-H1 window gives scores identical to full history. D1 yields ~50 points (weekend bars use ~29% of the fetched depth, outstanding item 18), H4 90. Context, not a signal. |
| **Real-FX-week bars (`fx_week.py`, outstanding item 18)** | ✅ RE-ENABLED 2026-09-19 with the Sydney->UTC conversion (`fx_week_v2`); validated against TradingView (5/6 exact) — see item 18 | Closed-market rows dropped before anything is built; persistent H1 history keeps the frozen D1 scorer at ~247 bars (>= 210) with no extra API credits; migration guard suppresses the first scan's edge-triggered pushes. 13 tests; full offline scan verified on real data. See item 18 and `scanner/FROZEN.md`. |
| **Hourly recommendation ranking + edge-triggered alert** (Signals Roadmap §5b) | ✅ | `scan_h1.py` now calls the frozen `rank.py::rank_pairs()` itself every hourly scan (right after `out` is assembled) and overwrites `ranked.top` — was previously only refreshed on `scan_news.py`'s slower news-cadence alongside the AI narrative, even though every input `rank_pairs` reads is already recomputed fresh each hour (only the 10%-weight cross-asset component still reads the preserved `macro_assets`). `ranked.text`/`.updated` untouched, still news-cadence — verified nothing in the app reads `ranked.text` before choosing that split. New edge-triggered `type: "recommendation"` push (local `_recommendation_alerts` in `scan_h1.py`, not added to `state_alerts.py`, to keep the diff small alongside a concurrent branch also editing `scan_h1.py`) — fires once when a pair newly enters `ranked.top` or flips direction while staying in it; no `prev` never fires. **Same-day follow-up (2026-09-17):** the original flat `ranked[:3]` swapped for a score floor, `RECOMMENDATION_MIN_SCORE = 6.5` (duplicated in `scan_h1.py` and `scan_news.py::call_ranked_analysis`) — a live check found 9 of 12 pairs can clear `rank.py`'s own `cont >= 45` gate on a strong trending day (one correlated USD theme, not 9 independent setups), so a flat top-3 either hid real extra setups or just papered over a flood. 6.5 picked by simulating against 40 scans of historical `ranked.top` scores (5% would show zero, vs. 7.0's 25%). No app-side change needed — HOME's glyph row and Watchlist's chip already iterate the list with no count assumption. 4 new tests, 69/69 extend suite green, `test_rule1_frozen` green (`rank.py` untouched, imported read-only). **Bugfix (2026-09-17, found by Pieter — "5 chips appeared, no notification"):** despite the design above, `scan_news.py::main()` was *also* still writing `ranked["top"]` (line 1129, `{**ranked_out, ...}`) on its own ~2h cadence, from `call_ranked_analysis`'s own internal score-floored ranking (added the same day as the score-floor change, for the catalyst-check call's context) — a second, silent writer to a field `scan_h1.py` was supposed to own exclusively, with no edge-trigger at all on that path. Confirmed live: `2bf1785` (a news scan) introduced AUDUSD/USDCHF into `ranked.top` with zero alert; the next h1 scan (`3aa1097`) saw them as already-present and correctly stayed silent. Confirmed on-device: zero `"recommendation"`-type entries ever recorded in `atomfx_notification_history.xml`, while Regime/Setup alerts from the same window recorded fine (ruled out a delivery/Doze issue). Fix: `scan_news.py` no longer writes `top` into `signals["ranked"]` — only `text`/`updated`, restoring single-writer ownership to `scan_h1.py` as originally documented. `test_rule1_frozen` + 73/73 extend suite green (no test changes needed — the existing `_recommendation_alerts` tests cover `scan_h1.py`'s side, which was already correct) |
| **Alert push title standardization** (2026-09-17, Pieter's ask) | ✅ | Every alert type's push message now leads with one consistent title shape, `"{Entity} — {Category} Alert"` (Entity omitted for the two market-wide types, Gold Signal and Regime), the category word matching its own Settings toggle exactly, specifics moved into the body. Touched `scan_h1.py` (Gold Signal, Recommendation), `scanner/extend/state_alerts.py` (Setup, Structure, Regime ×2, Volatility, Alignment, BB Touch), `scanner/extend/conviction.py` (Positioning) — all EXTEND/call-site tier, no `type` string or computation changed. Setup's message also drops its old "entered Tradeable Now" line (a stale reference to the long-retired card) as a side effect. **Level Alert is the one exception, deliberately skipped** — its message lives in `level_ema_alerts.py` (FROZEN) and the feature is dormant (Settings toggle hardcoded disabled); still reads `"Level Alert — {pair}"` (pair second). See item 7 below for the note to align it once that feature actually ships. `test_rule1_frozen` + 69/69 extend suite green |

---

## B. Android app (Kotlin/Compose) — by surface

| Surface (spec ref) | Status | Where / what remains |
|---|---|---|
| Energy Wheel — pair mode (Design §6) | ✅ | `ui/wheel/*` (radial dial v2). 4 selectable wings, renamed 2026-09-05: **Setup** (labelled "SETUP" on the wheel since 2026-09-09, was "OVERALL" — internal `WheelMode.OVERALL` enum name unchanged; Continuation Score, all TFs), **Trend** (ADX, fixed H4 — no D1/H1 ADX exists), **Momentum** (fixed H4, switched from D1 2026-09-09), **Volatility** (ATR percentile, fixed D1). Hub/nucleus moved H4→D1 Regime 2026-09-09 ("the H4 Regime changes too much") — Regime/Trend/Momentum/Volatility now form a deliberate fixed-timeframe consensus funnel, never a D1/H4/H1 toggle (tried twice, reverted both times, see `[[atom_fx_simplification_rework]]`) |
| Currency strength on the wheel (§3A) | ✅ | Merged into the wheel's Currencies/Pairs corner toggle (wheel v2 decision); always-on `CsmBarStrip` below the dial too, with a Strength/Flow display toggle |
| Cross-asset sheet | ✅ | **Correction 2026-09-10**: the wheel's own outer cross-asset ring was retired 2026-09-06 (`14b8e54`), not "unchanged" as this file previously said — checked directly, `WheelCanvas.kt` has zero cross-asset code left. `ui/sheets/CrossAssetSheet.kt` itself is still fully live, just reached from the Macro screen's own cross-asset cards now, not the dial |
| Pair sheet (Phase 4) | ✅ | `ui/sheets/PairSheet.kt` — reworked 2026-09-05/09 from a 6-factor pass/fail WHY checklist to 3 tabs: **Overview** (5 informational rows — Regime D1/Trend H4/Momentum H4/Volatility D1/Structure H4, real bull/bear/watch tints, no gate glyphs), **Breakdown** (Momentum, Structure, Alignment), **Correlation**. `FlowSheet`/`BreadthSheet`/`EntrySheet`/`RecommendationSheet`/`CurrencyFlowSheet` are all gone — deleted outright, `BreadthSheet.kt` included (**corrected 2026-09-17**: this row previously said the file still existed as unreachable dead code; checked directly, `find`/`grep` across the whole `app/` tree confirm zero references anywhere — the cleanup outstanding item 11 flagged already happened, just never got checked off here). **2026-09-17 (full-system audit):** Breakdown → Structure's "Multiplier (H4)" row added — `scanner/structure.py`'s 0.40–1.30 score multiplier was already computed and already documented in the Library, but the value itself was silently dropped by the Kotlin model and never shown; now a plain row right after Strength (H4), same style. Verified on-device (EURUSD live data) |
| Status strip / ranked-pairs glyph row (Phase 5) | ✅ | `ui/components/StatusStrip.kt` — was a 9-button cascade, then a single "Summary→Recommendation" card, now (2026-09-06) one small glyph per pair in `signals.ranked.top` above the wheel — **corrected 2026-09-17: no longer count-capped** (was ≤3 until the 2026-09-17 score-floor change, row 48 above; contradicted that row here until this fix), just every pair scoring ≥6.5; tap reflows open a full-width panel with that pair's Regime/Trend/Momentum/Volatility/Structure consensus + rank. There is **no separate "Tradeable Now" card any more** — that job is now this glyph row plus the wheel's own Setup wing (all 12 pairs, wedge size = Continuation Score) |
| Line chart, 3-TF, no candles (Phase 9) | ✅ | `ui/chart/LineChart.kt` — still used by the pair-sheet header's always-visible `Spark3Row` D1/H4/H1 sparklines. **`ChartSheet` (long-press) no longer uses it** — moved 2026-09-10, see the %B row below |
| Macro screen (Phase 9) | ✅ | `ui/macro/MacroScreen.kt` (archetype, bias, evidence, cross-asset table). A reframe was raised and explicitly deferred 2026-09-05 ("the archetypes do not describe what one sees happening… evidence bars look nice but aren't evidence of anything") — **resolved 2026-09-10, see the Macro Archetype rework row below** |
| **Macro Archetype rework** (2026-09-10, Pieter's ask — resolves the 2026-09-05 deferral above) | ✅ | Two-part fix for "the name should accurately reflect reality, and the conclusion should be actionable" (the live example: the page read "Oil supply shock" with no actual oil supply shock happening). **(1) Renamed all 10 `REGIME_LIB` archetypes** — see `macro_regime.py`'s own doc comment for the full per-code rationale; short version: names now describe the observed cross-asset PATTERN, not an inferred CAUSE the classifier's price-only inputs can't verify, and dropped "shock" language from the 5 codes that don't require an actual same-day volatility spike to fire (kept it for E/J, which do). Diverges deliberately from the FX Macro Flow Handbook's own A-J names — Functional Spec §6's regime table carries the full cross-reference. Every playbook/alert-copy reference updated too (`RegimePlaybookContent.kt`, `AlertPlaybookContent.kt`, `TechnicalRegimePlaybookContent.kt`, `fixtures/`, `tools/make_ui_fixtures.py`). **(2) Implemented the redesign proposal**: `_confidence()` is now margin-aware — a tie with the runner-up on distinct axes force-caps confidence to Low, surfaced on the page as an explicit "tied with X" callout instead of a bare pill; `scan_news.py`'s own `tag_theme()` headline-axis tags (previously "colour only," see the row above) now feed a new `news_corroboration` field ("confirmed"/"price_only"/"unknown"); `MacroScreen.kt` reordered to lead with the purely-descriptive Evidence axes, demoting the archetype name from a Title-weight hero to a hedged "Best match: {name}"; currency-bias baskets now check each currency against its own live H4 CSM (CSM=50 split, same convention Rotation uses) and mute+annotate ones the technicals don't currently back. New `macro_regime.news_corroboration` + `macro_regime.secondary.distinct_axes` fields (additive), `schema_version` 6→7. 3 new/extended tests (tie-confidence, news corroboration confirmed/price-only/unknown), 66/66 extend suite green, Rule #1 holds. Verified on-device (crafted a tied/price-only/mixed-CSM scenario, confirmed every new UI element renders correctly) |
| Push client (Phase 6) | ✅ | `push/AtomFxMessagingService.kt` + `DeepLink.kt`. Message format changed 2026-09-0x: `send_push.py` now sends **data-only** FCM (was combined notification+data, which the system tray auto-displayed when backgrounded/killed — bypassing the app's own `onMessageReceived` and the new Notification History hook entirely) |
| **Notification History screen** (new since last audit) | ✅ | `ui/settings/NotificationHistoryScreen.kt` (Settings → Notifications → "Notification history") — every push the app builds is recorded on-device, flat reverse-chronological list, per-alert-type label chip (GOLD SIGNAL/LEVEL ALERT/SETUP/STRUCTURE/REGIME/MACRO ARCHETYPE/VOLATILITY/ALIGNMENT/POSITIONING), each with a static "what to consider" line + "Learn more" deep-link into the matching Library entry, unread dot on the header gear. **Correction to an earlier memory**: this is a flat list, not grouped under REGIME/TREND/VOLATILITY/STRUCTURE/OTHER section headers — that description was of a screen this one superseded |
| **Regime/Alert Playbook + Reading Window** (new since last audit) | ✅ | `ui/sheets/TechnicalRegimePlaybookDetail.kt` + `AlertPlaybookDetail.kt` — Pieter's "living handbook" vision: contextual explanations grounded in the actual live regime/archetype/alert mechanics (real vote composition, real evidence axes), not static copy, wired first to `archetype_change` then extended to 5 more alert types. A book+ icon at heading level opens a dedicated full-screen **Reading Window**, deliberately separate from the sheet system ("sheets inspect data, a [Reading Window] teaches") |
| **Watchlist** (new since last audit, Signals Roadmap §5) | ✅ | `ui/watchlist/WatchlistScreen.kt` + `data/WatchlistStore.kt` (SharedPreferences + JSON + hot `StateFlow`, no new dependency) — reached via a new header bookmark icon (sibling slide-in panel to Settings, not nested/not a bottom sheet). Add/remove a pair from its own pair-sheet header; tap-through to the full pair sheet. **Redesigned 2026-09-16** from a BB-touch-specific card (touching badge, ADX/Reset/band-width metrics, a "what to look for" reversal-checklist reveal) to a general pair-state card: pair + direction word (LONG/SHORT/—) + added time, a "★ RECOMMENDED LONG/SHORT" chip when the pair is currently in `signals.ranked.top`, a "Setup {cont} · {state}" ripeness line (Continuation Score + the six-factor potential state when present), the same five-factor Regime/Trend/Momentum/Volatility/Structure consensus dot row StatusStrip's own recommendation panel shows (now one shared `ui/components/ConsensusRow.kt` implementation, extracted so the two surfaces can't drift — this is also where the VOL dot's blue/amber-not-green fix now lives), and the D1/H4/H1 pill row |
| **Header green dots — watchlist + calendar (2026-09-10, Pieter's ask)** | ✅ | `MainActivity.kt` — the gear's own unread-notification dot (7dp `CircleShape`, `colors.bull`, `TopEnd`-aligned) extended to the watchlist and calendar glyphs, factored into a shared `HeaderGlyphWithDot` composable (was 3 near-duplicate inline `Box`es). Watchlist dot: `WatchlistStore.state.isNotEmpty()`. Calendar dot: any `calendar.events` entry whose `iso` timestamp is within the next hour (`OffsetDateTime`/`Duration`, same parsing `CalendarSheet.kt`'s own countdown already uses) — threshold confirmed with Pieter (1h, matching the app's own hourly scan cadence) rather than assumed. Both verified on-device |
| **Trading Sessions (2026-09-17, Pieter's ask)** | ✅ | A fifth header icon (clock glyph, `SessionsGlyph` in `MainActivity.kt`) opens `ui/sessions/SessionsSheet.kt` — same slide-in side-panel recipe as Settings/Watchlist/Calendar, but the only one of the five that reads no `signals`/`loaded` at all: session hours are a fixed calendar convention, not a trading number, so this is a pure clock feature with zero `signals.json` involvement (`domain/TradingSessions.kt`). Each of Sydney/Tokyo/London/New York is defined in that city's own real IANA timezone (`Australia/Sydney`/`Asia/Tokyo`/`Europe/London`/`America/New_York`), not a fixed UTC offset, so the platform's own timezone database — not hand-rolled DST math — decides how each session's UTC-equivalent hours shift across the year (Tokyo has no DST at all; the other three each observe their own region's DST on their own schedule). Hours are the commonly-cited retail convention (Pieter's call, after flagging that "session hours" is itself a source convention, not one universal fact). Sheet shows a live-ticking (1s while open) 24h rolling timeline bar plus per-session exact device-local open/close time and countdown; header dot lights on a session OVERLAP specifically (London-NY, Tokyo-London), not "any session open," since some session is open most of the day regardless and that alone would light almost constantly. Verified on-device against an independent manual calculation accounting for each region's current DST state (BST/EDT active, Tokyo/Sydney not) — every displayed time matched exactly |
| Theme tokens, dual light/dark (Design §2) | ✅ | `ui/theme/*`, unchanged |
| Animations & polish (Phase 8) | 🟡 | Wheel has motion (mode cross-fade, wedge fill animation, glyph-panel reflow), verified on-device per the Simplification Rework follow-ups. **Correction 2026-09-10**: there is no rim-glow/flash treatment for A+ pairs — checked `WheelCanvas.kt` directly, its own comment says "indicated by its fill/border alone, no separate rim glow treatment" (this file previously claimed one existed, based on stale `ATOM_FX_WHEEL_V2_SPEC.md` §11 content, not verified code). One acknowledged real gap: the `CsmBarStrip` is still plain bars, not yet the "echoes the wheel" visual pass (`WheelScreen.kt`'s own doc comment) |
| **3-tab bottom nav + swipe (HorizontalPager)** | ✅ | `MainActivity.kt` — `Wheel · Macro · Insights` via `HorizontalPager` + Material 3 `NavigationBar`, synced both ways |
| **Insights tab/screen (§7, master table 46–50)** | ✅ | `ui/insights/InsightsScreen.kt` — recommendation card + breaking headlines + catalyst check + calendar + daily brief + week ahead, one scrolling screen. A rethink was raised and explicitly deferred 2026-09-05 ("ask ourselves how we can use it better, if at all") — open question, not a defect |
| **Settings screen (§9, header gear)** | ✅ | `ui/settings/SettingsScreen.kt` — live `system/dark/light` override, notification master + per-type toggles + send-test + **Notification History** (new), data-source URL, freshness/diagnostics, About. Price-level alerts present but disabled — see next item |
| **Header gear + Insights icon (§3.1)** | ✅ | Persistent gear strip above the `HorizontalPager` on all 3 tabs → opens `SettingsScreen` full-screen. Regime name moved onto the wheel's own hub (no longer in the header); recommendation now lives in the status-strip glyph row, not the header either. The hub's `strengthWord`/`flowLine` fields (`WheelMapper.mapNucleus`) are computed but deliberately not rendered — Pieter's own earlier call, decluttering `WheelCanvas.drawHub` down to just the regime name |
| **Notification per-type filtering** | ✅ | `AtomFxMessagingService` checks `UserPreferences` before showing a notification — the only enforcement point, since the backend still sends everything to one shared topic |
| **Price-level alerts UI (optional, §9/§12)** | 🟡 | Server *fires* level alerts already (`send_push_level_alert`). Settings has a disabled placeholder row; the on-device "set alert" row on the pair sheet + PAT sync to `data/level_alerts.json` is still not built |
| Firebase Android registration end-to-end | ✅ | **Confirmed 2026-09-10** — manually triggered `test_push.yml` (`python -m push.send_push`, a real Firebase Admin SDK call) and watched the device: the real notification arrived and was correctly recorded at the top of Notification History. Full path verified — GitHub Actions → Firebase → FCM → device → `onMessageReceived` → posted → recorded. History's own list also already had real Setup/Regime/Structure alerts from live scans, independent evidence the pipeline works for real signals too |
| **Journal / trade-thesis worksheet (§12)** | 🅿️ | Post-v1 (Phase 10+). Pre/post-trade forms from handbook Appendices C/D |
| **Whole Market Indicators card (Insights tab, Design §19.4)** | 🗑️ removed | Built, restyled (picker fan-out), extended to 5 items (Currency %B), then removed **same day** once Currency %B proved to be the useful one of the five ("I think this might be a very useful indicator... I think you can remove the complete 'Whole Market Indicators' card" — Pieter's own call). `MarketIndicatorsCard.kt` + its 5 dedicated chart files (`RotationChart.kt`/`PulseChart.kt`/`ThrustChart.kt`/`PercentBBoardChart.kt`/`CurrencyPercentBChart.kt`) deleted outright; `Signals.kt`'s now-unused `rotation`/`pulse`/`breadthThrust`/`percentBBoard` fields (and `RotationBlock`/`PulseBlock`/`ThrustBlock` types) removed too rather than left dead. See Design §19.4 for the full retirement note |
| **%B 12-period, per-pair (Design §19.4a)** | ⏸️ deferred (backend live) | **2026-09-18 — the CHART is deferred, not deleted** (Pieter: "should not be discarded, maybe just deferred for now, I have an idea for the BB touch alerts I want to discuss later"). `ChartSheet` now shows the stock-standard 20-period %B instead (row below); this 12-period read is the BB touch alert's own band math, and two %B charts on one sheet on different periods invite a meaningless comparison. `bb_touch.py`, `pairs.<PAIR>.bb_d1` and `ui/sheets/PercentBChart.kt` are ALL untouched and still in the repo — do not delete as dead code. Original build notes follow. | `ui/sheets/PercentBChart.kt` + rewritten `ChartSheet.kt`. Started the same day as a 4th Pair-sheet `BreakdownSection`, then moved the same day (Pieter's follow-up ask) onto the wheel's long-press sheet, replacing that sheet's old D1/H4/H1 price `LineChart` (a duplicate of the header's own always-visible `Spark3Row`) and its now-purposeless TF switcher — %B is D1-only. `ChartSheet` now shows a `%B <value>` + band-touch-state header row above the same shared `ui/chart/PercentBOscillator.kt` drawing every %B chart in the app uses. Verified on-device both ways: opens correctly on long-press, and the Breakdown tab no longer shows it. **Same-day follow-up (schema v5):** Pieter caught the value itself not matching a live LiteFinance USDJPY %B read — traced to `bb_d1` reading the frozen aggregator's UTC-midnight D1, while retail platforms close D1 at 17:00 New York; `bb_touch.py` now builds its own D1 via `_d1_ny_close()` (independent re-bucketing of `raw_ohlcv`, same Rule #1-safe pattern the dates fix used, never touches the frozen aggregator). Confirmed fixed against a subsequent real scan |
| **Currency %B (`percent_b_currency`, schema v6)** | ✅ | `bb_touch.py`'s new `compute_currency_percent_b()` — Pieter noticed Board %B mixes every pair's raw %B with no regard for base/quote (EUR/USD falling = USD strong, USD/CAD falling = USD weak, averaged together unfixed). Same base/quote correction `csm.py`'s own `compute_csm_d1` already does, mirrored around 100 instead of negated (%B is a 0-100 position, not a signed return). Uses `csm.py`'s `STRENGTH_PAIRS` (18 pairs) for full currency coverage, independently computed from `raw_ohlcv` rather than reading `pairs.<PAIR>.bb_d1`. 65/65 tests pass (2 new, incl. a hand-verified base/quote sign-correctness check), Rule #1 holds. **UI surface (since 2026-09-18): the bottom of `CurrencyDetailSheet` only** — one card per currency, reached by tapping a CSM-strip bar. Moved there from ChartSheet when the pair's own 12-period %B card was deferred, since the side-by-side comparison it was originally placed for no longer exists on that sheet. The composable itself now lives in `PercentBChart.kt`. Originally (2026-09-10): **`ChartSheet.kt`'s `CurrencyPercentBCard`** — the pair's own base+quote currencies, each a small card below the pair's own %B, so a currency-level fragility read can be checked against whether the pair's other leg agrees (Pieter's own workflow, talked through first, then built). A same-day Insights-tab picker surface (`CurrencyPercentBChart.kt`) existed briefly too, but was removed along with the rest of the "Whole Market Indicators" card once this pair-level one proved to be the useful surface. Verified on-device |
| **Momentum — RSI/MACD (`momentum_series`, schema v9)** | ✅ | `scanner/extend/momentum_series.py` — `attach_momentum_series()` keeps a 90-point tail (up from 50 — display cap only, 5000 H1 bars already covers far more) of `score.py`'s own frozen `_rsi`/`_macd` outputs per pair, per D1/H4/H1, instead of discarding history after scoring (Rule #1-safe: reads the frozen functions, computes nothing new). First half of a 4-indicator glance panel (Pieter's ask, "a set of standard indicators... each gives one a different view") — shipped alone first so the visual result could be checked before building the rest; **panel completed 2026-09-18, see the `bollinger_series` row below**. **2026-09-18 fix (Pieter's catch, "skewed vs LiteFinance")**: D1 now runs on `_d1_ny_close()`'s 17:00-NY-session bars, not the frozen aggregator's UTC-midnight D1 — same root cause the %B fix already addressed. H4/H1 stay on the frozen aggregator's own bars (already correct; no universal broker-agnostic boundary to re-derive). Every series also gained its own `dates` — D1 via the same NY-close call, H4/H1 via a new `scanner/extend/tf_dates.py` that recovers just the timestamps the frozen aggregator discards (cross-checked against its own close values in tests, not just row count). **2026-09-18 restyle (Pieter's ask, "very flat")**: `ui/chart/MomentumOscillators.kt`'s `RsiOscillator`/`MacdOscillator` — MACD's histogram now gets its own vertical scale independent of the MACD/signal lines (a histogram is a difference of two similarly-sized series, so a shared scale flattened it to a sliver); RSI shades the 30/70 band; both charts are taller, finer-stroked, show date labels, and add a soft glow behind the current-value endpoint dot (same `BlurMaskFilter` technique the wheel's own hub glow uses, `WheelCanvas.kt::glowFillCircle`). **UI layout**: `ChartSheet.kt` now gives RSI and MACD each their own card (was one shared "Momentum" card), with a bare full-width equal-thirds D1/H4/H1 row (no title) driving both — since 2026-09-18 they are cards 3 and 4 of the completed panel, below %B (20) and BandWidth, and that same row drives all four. 9 new/updated extend tests (dates alignment, H4/H1 date-recovery cross-checked against the frozen aggregator's own close values, D1 NY-close cross-check), 83/83 extend suite green, Rule #1 holds. Verified on-device against a real scan (`95481ec`, then the restyle re-verified against the same live cache) — EURUSD D1/H4/H1 all render correctly, TF switch updates both cards together |
| **%B (20) + BandWidth (`bollinger_series`, schema v10)** | ✅ | `scanner/extend/bollinger_series.py` (2026-09-18) — **completes the 4-indicator glance panel**. Standard 20-period ±2σ bands over the closes already fetched this scan, keeping a 90-point tail of %B, its 20-period signal SMA, BandWidth ((upper−lower)÷middle×100) and a per-bar `squeeze` flag, per pair per D1/H4/H1. Same bar conventions `momentum_series` established (D1 on `_d1_ny_close()`'s 17:00-NY session, H4/H1 on the frozen aggregator's bars, dates via `tf_dates.py`). **Deliberately a separate module from `bb_touch.py`, not a re-parameterisation of it** — the alert's 12-period bands are the alert's own parameter (Pieter specified 12 explicitly) and must not move to serve a chart; a test asserts `bb_d1` is byte-identical before and after this module runs. **Squeeze = BandWidth at its lowest in 125 bars**, John Bollinger's own published definition — Pieter's explicit call over inventing an untuned percentile cut-off. Chart: new `ui/chart/BandWidthChart.kt` (data-driven Y domain, since BandWidth has no universal "wide"; `watchSoft` column + `watch` dots for squeeze bars), %B reuses the shared `PercentBOscillator`. `ui/chart/ChartCommon.kt` extracted the primitives all four charts now share. 8 new extend tests (20-vs-12 cross-check, squeeze correctness both ways, bb_d1 non-disturbance, dates alignment, NY-close D1, fail-quiet), **91/91 extend suite green, Rule #1 holds**. Verified on-device against real cached H1 bars (EURUSD D1 + H4 both render, squeeze marks appear where expected, values match the computed series) |
| **M15 glance-panel chart (`scan_m15.py`, schema v11)** | ✅ live | New `scanner/scan_m15.py` (2026-09-18, Pieter's ask, "just as a chart" on the pair bottom sheets) — a 100% NEW-tier orchestrator (no frozen ancestor, same shape as `scan_cot.py`), on a genuinely separate ~45-min cadence from `scan_h1.py`'s own 2h one. Fetches M15 OHLCV for the 12 wheel pairs only (not the 6 `CSM_EXTRA` pairs — chart-only, no CSM/scoring use) and **merges** a new `"m15"` key into each pair's existing `momentum_series`/`bollinger_series` maps, never replacing them. Runs the same frozen `_rsi`/`_macd` and the same EXTEND-tier band math already used for the other three timeframes. New top-level `m15_updated` timestamp, deliberately separate from `updated` — not yet surfaced in the app UI. **Credit budget, Pieter's own call after walking the free-tier math**: 12 pairs × ~32 runs/day ≈ 384 credits/day on top of scan_h1.py's unchanged 144/day = 528/800. New `.github/workflows/scan_m15.yml`. **App side**: `ChartSheet.kt`'s TF row is now D1/H4/H1/M15 (zero model changes — "m15" is just another map key). **Apps Script trigger added and confirmed live same day** — a second `everyMinutes(15)` trigger self-pacing to ~45 min via a stored timestamp, weekday-gated. Confirmed via a real `chore: m15 scan` commit from `github-actions[bot]`. **Same-day production bug, found via that live check and fixed before lasting data loss:** `scan_h1.py` rebuilds `pairs_out` fresh every run and its `attach_momentum_series`/`attach_bollinger_series` calls REPLACE each pair's whole series dict (d1/h4/h1 only) — silently erasing the `"m15"` key on the very next `scan_h1.py` run, confirmed in production (the `m15 scan` commit's own data was gone by the following `h1 scan` commit). Same bug at the top level: `m15_updated` wasn't in `PRESERVED_KEYS`. **Fixed**: both `attach_*` functions now take an optional `prev_pairs` argument and carry the `"m15"` key forward before overwriting the rest; `m15_updated` added to `PRESERVED_KEYS`. Full reasoning and the exact production evidence in `ATOM_FX_ARCHITECTURE.md` §4.2. 97/97 extend suite green (5 new: the merge-safety trio + 2 carry-forward regression tests), Rule #1 holds. Verified against a simulated replay of the exact production sequence that exposed the bug, not just the isolated unit tests |
| **Crowd score card (ChartSheet, Roadmap §5c)** | ✅ built and verified on device (2026-09-19; on `main`) | `ui/sheets/ChartSheet.kt::CrowdScoreCard`, `ui/chart/CrowdScoreChart.kt`, `crowdState` (`SheetComponents.kt`), `CrowdSeries` in `Signals.kt`, Library entry. Fifth card, below MACD, D1/H4 only, red/green top/bottom lines, dashed 60, footer state word (`No COT` outranks it). Unit-tested (`CrowdScoreTest`, 11 cases). **Verified on a physical device** (Android 16) against a test `signals.json` built from the live snapshot plus real module output for all 12 pairs, with pairs forced into each state: all five footer states render with the right word and tint (Crowded top, Crowded bottom, Mixed, Not crowded, and `No COT` outranking a Crowded top), the flag dot appears at the first bar over 60, the card shows on D1 (~50 points) and H4 (90) and is absent on H1 and M15, and it reads correctly in dark and light. Design §19.4b. |
| **Crowd score alerts (Roadmap §5c Phase 2)** | ✅ built (2026-09-19); Settings UI verified on a physical device, the push itself not yet seen live | `state_alerts.py::_crowd_score_alerts` (`type: "crowd_score"`, rising into a higher band 1–19/20–39/40–59/60+ on a completed D1 or H4 bar, one push per pair × timeframe × side) + `push/CrowdAlertFilter.kt` (the person's per-timeframe minimum Any/20/40/60, applied on the device because the topic is shared) + `NotificationPrefs`/`UserPreferences`/Settings rows, history label `CROWD SCORE`, `ALERT_GUIDANCE`, Library entry. **Default OFF** (D1 40 / H4 60 once on). 7 backend tests + `CrowdAlertFilterTest` (5). Volumes measured on real history; judgment calls listed in Roadmap §5c §4. **Not yet seen live:** it needs one scan after the first `fx_week_v1` scan to have a previous `latest` to compare against. |
| **Glance-panel visual unification (Design §19.4b, 2026-09-18 3rd restyle)** | ✅ | Pieter's ask, "let them all look similar": all four cards now share one two-tone shell (grey `surfaceRaised` header strip + black `ground` plot area, `ChartSheet.kt::IndicatorCard`), the pair name dropped from %B's own header (the sheet title already names it), %B/RSI's threshold-line styling unified (RSI's shaded 30/70 band removed to match %B's plain dashed treatment), every line white except MACD's own signal line (RSI no longer bull/bear-tinted, BandWidth no longer watch-tinted on squeeze — its dots/column still are), %B's own signal (SMA) line removed from this card specifically (Currency %B, a different sheet, keeps its own), %B's endpoint now glows matching the other three. **Real bug found and fixed in the same pass**: the shared date-row helper tried `LocalDate.parse` on every date string, which throws on M15's full ISO-datetime strings — caught and silently swallowed, so every M15 chart's date row was rendering nothing. Fixed: falls through to a clock-time label (`HH:mm`, UTC → device timezone, the same conversion pattern already used elsewhere in the app) when a bare date fails to parse. Also: the sheet can no longer be swipe-dismissed (`BottomSheetHost.kt`'s `confirmValueChange` gates only the drag-to-hidden transition for `SheetTarget.Chart` — back-press/scrim-tap, a separate path, still work) — a new "Close" text, top right, is the explicit way out. Also caught and fixed in the same pass: `LibraryContent.kt`'s %B entry still opened with "12-period ±2σ D1 bands," stale since the 20-period rework and contradicting its own later sentence; RSI's entry still described the now-removed shaded 30/70 band. Verified on-device: header strip/black-body split, all-white lines, M15 real time labels (was blank), glowing %B endpoint, a fast flick-down leaving the sheet open (scrolls instead), and the Close text/back-button both correctly dismissing it |
| **Glance-panel footers + Currency %B makeover (Design §19.4b, 2026-09-18 4th restyle)** | ✅ | Pieter's ask, "every chart should have some sort of info at the bottom, for uniformity," plus "the same makeover" for Currency %B. `IndicatorCard` gained a `footer` slot (a third grey strip below the black plot area) and, with `LegendItem`, moved from `ChartSheet.kt` to `SheetComponents.kt` once `PercentBChart.kt` needed the same shell — folding in what had been two near-identical `private LegendItem` copies. BandWidth's and MACD's existing legends moved into the new footer (content unchanged); BandWidth's "In squeeze" word moved out of the header's trailing slot into the footer alongside its own legend. %B's footer is the removed signal line's own reading as plain text ("Signal (SMA 20) 68"); RSI's footer spells out Overbought/Oversold/Neutral in words (always present, never blank) — the same read the 30/70 dashed lines already show visually, same upper-is-bear/lower-is-bull tint convention the app already uses elsewhere. **Currency %B (`CurrencyPercentBCard`, `CurrencyDetailSheet`)** got the identical `IndicatorCard` shell — black plot area, grey header reading `%B (12) <value>` (currency code dropped, the sheet's own title already names it), grey footer with its existing %B/Signal legend — and the old sentence-style explainer text is gone outright (not moved). Also removed: the now-redundant outer "%B" section-header caption `CurrencyDetailSheet.kt` printed above the card, matching its DRIVERS/EXPRESSED BY convention — duplicated the card's own new header once that existed. Currency %B's chart itself is otherwise untouched (still draws its own 12-period signal line; that removal was glance-panel-specific, never asked for here). Every chart's date/time row stays inside its own black Canvas, not moved into either grey strip (Pieter's explicit ask). `LibraryContent.kt`'s %B/BandWidth/RSI-MACD/Currency-%B entries updated to describe the new footers. Verified on-device against a full synthetic fixture (real `_series_for` output across D1/H4/H1/M15 for two pairs, plus `percent_b_currency` for three currencies, including a deliberately narrowed BandWidth tail to confirm the squeeze footer/marks actually fire): every footer renders real content, Currency %B's new header/footer/no-explainer all correct with its own signal line still drawn |
| **Glance-panel footers answer "what is the graph FOR" (Design §19.4b, 2026-09-18 5th restyle)** | ✅ | Pieter's ask: before coding, answer per card what it's actually FOR, then make the footer say that instead of a supporting number. Agreed per card: %B/RSI both ask "stretched or normal" (position-in-band vs. momentum-velocity); BandWidth asks "quiet or normal" (volatility); MACD asks "which way, building or fading" (direction/turn — a genuinely different question). **%B's footer** changed from the removed signal line's own reading to a state word — "Stretched high"/"Stretched low" (bear/bull-tinted, off the exact 10/90 lines the chart already draws, no new threshold) or "Normal range" — via a new shared `percentBState` helper (`SheetComponents.kt`), used by both %B(20) and Currency %B. **BandWidth's footer** changed from conditional ("In squeeze" only when true) to always-present ("Quiet (squeeze)"/"Normal") — deliberately no third "Expanding" state, since that needs an untuned width-trend threshold this project has no data to set yet, same caution `bb_touch.py`'s own `width_trend` already flags about itself. **MACD's footer** gained a new `macdState` helper (`ChartSheet.kt`): direction from the histogram's own sign (matching the bars' bull/bear tint), "building"/"fading" from a plain sign comparison of `abs(histogram)` bar-over-bar — not a magnitude threshold. **RSI's Overbought/Oversold/Neutral** (shipped 4th pass) already answered its own question correctly, unchanged. **Currency %B** got the same `percentBState` treatment as %B(20), added alongside its existing %B/Signal legend (not replacing it — that chart still draws two real lines needing a legend, unlike %B(20)'s single line). `LibraryContent.kt`'s four affected entries updated. Verified on-device with a deliberately constructed fixture (a quiet walk then a sustained rally, plus a second pair with an *accelerating* rally) to force each state to actually fire: confirmed "Stretched high" (%B=90), "Overbought" (RSI=99), "Bullish, fading" (shrinking histogram), and "Bullish, building" (growing histogram) all render with the correct word and tint against real computed values |
| **Glance-panel footers simplified to just the state word, right-aligned (Design §19.4b, 2026-09-18 6th restyle)** | ✅ | Pieter's ask: remove any text on the footers except the state describing text, and right-align it. `IndicatorCard`'s `footer` slot (a free-form composable) became `footerState` (a plain `Pair<String, Color>?`), rendered as one right-aligned `Text` in a `Box(contentAlignment = Alignment.CenterEnd)` — a direct simplification of the shell, not just a per-card content edit. Every caller's own legend is gone as a consequence: BandWidth's "Squeeze (125-bar low)", MACD's "Signal"/"Histogram", Currency %B's "%B"/"Signal (SMA 12)" — all removed outright, leaving exactly the 5th pass's own state word on each card, nothing else. `LegendItem` (`SheetComponents.kt`) stays defined — the deferred, untouched 12-period pair %B card (`PercentBChart.kt::PercentBChart`) is its one remaining caller. Also cleaned up in the same pass: `ChartSheet.kt`'s now-unused `Row`/`Arrangement` imports. `LibraryContent.kt`'s five affected entries updated to note the right-alignment and the removed legends. Verified on-device against the same purpose-built fixture as the 5th pass (device briefly disconnected mid-session, reconnected before verification): all five footers ("Stretched high", "Normal", "Overbought", "Bullish, fading", "Normal range" on Currency %B) confirmed rendering alone, flush right, no legend, no dot |
| **RSI dashed lines matched to %B's own pixel height (Design §19.4b, 2026-09-18 7th restyle)** | ✅ | Pieter's ask: the RSI thresholds should appear the same distance from the centre line as %B's. New shared `THRESHOLD_LINE_LOW`/`_HIGH` (`ChartCommon.kt`, 10/90) — RSI's dashed lines now draw at these values instead of RSI's own literal 30/70 (both charts share the identical `chartHeight`/`py()` formula, so 10/90 vs. 30/70 previously put the two reference bands at genuinely different heights, 80% vs. 40% of the way from centre to edge). Deliberately not a domain rescale — RSI's value line still plots true 0–100 readings, and the Overbought/Oversold/Neutral footer state still reads the real 30/70; only the two drawn lines moved, now a purely visual reference band shared across the panel. `PercentBOscillator.kt` switched to the same shared constants too (was its own local `10.0`/`90.0` literal). `LibraryContent.kt`'s RSI entry updated to explain the line/footer-state can legitimately read "Overbought" before the (relocated) dashed line is reached. Verified on-device: RSI=99 confirmed rendering above the relocated upper dashed line while still correctly showing "Overbought" |
| **CurrencyDetailSheet reordered: Conviction · Breadth (H4) · Expressed By · Drivers** | ✅ | Pieter's ask — was Breadth · Conviction · Drivers · Expressed By. Header/CSM squares (top) and the %B card (bottom) untouched; only the four middle sections moved. `ATOM_FX_DESIGN.md`'s own sheet-inventory line and §20 acceptance-test mention both updated to match — the inventory line was also missing Conviction from its list entirely, caught in the same pass. Verified on-device against real live USD data: confirmed exact new order top to bottom |
| **Glance-panel bug audit (Pieter's ask, "indepth audit... rank order them, and fix them," 2026-09-18)** | ✅ | Systematic file-by-file read of every chart-related file, backend (`bollinger_series.py`, `momentum_series.py`) and frontend (`ChartCommon.kt`, `PercentBOscillator.kt`, `BandWidthChart.kt`, `MomentumOscillators.kt`, `ChartSheet.kt`, `PercentBChart.kt`, `SheetComponents.kt`, `Signals.kt`). Two real bugs found and fixed, ranked: **(1, medium-high) MACD's `macdState()` could report "fading" at the exact moment of a fresh bullish/bearish cross** — it compared `abs(last) > abs(prev)` unconditionally, so a small new bar on the *other* side of zero from a larger old one read as shrinking, when a sign flip should always mean "just turned, building." Fixed: checks `(last >= 0.0) != (prev >= 0.0)` first and forces "building" on any cross. **(2, medium) Three header "reading" numbers used `.toInt()` (truncates toward zero) instead of `.roundToInt()`** — `ChartSheet.kt`'s `PercentBCard`/`RsiCard` and `PercentBChart.kt`'s `CurrencyPercentBCard`, a systematic downward-biased display (e.g. 89.7 shown as "89"). Confirmed this never touched the underlying threshold/state logic (`percentBState`, RSI's own `when` block), which already read the raw un-rounded `Double` — display-only. Also applied, low-severity/defensive: `PercentBOscillator.kt`'s signal-line right-align offset (`n - signal.size`) now also guards `signal.size <= n`, preventing a theoretical off-canvas negative offset if a caller ever passed a signal series longer than its line (not reachable today — the backend's own construction already keeps `signal.size <= line.size`). New `ChartSheetTest.kt` (6 cases) locks in the sign-flip fix, including the exact "smaller bar, still a fresh cross" shape that was misread before. `LibraryContent.kt`'s RSI & MACD entry updated to describe the cross behaviour. 97/97 extend suite green, Rule #1 holds (Kotlin-only fixes; Python untouched). Verified on-device against **live production data, not a synthetic fixture** — EURUSD's real H4 MACD happened to be mid-cross at verification time (a small green bar right after a red streak) and correctly showed "Bullish, building"; %B/RSI header readings and Currency %B's %B(12) card all confirmed showing correctly rounded integers, no crash, no regression |
| **RSI's dashed thresholds reverted to its own real 30/70 (Design §19.4b, 2026-09-18 8th)** | ✅ | Pieter's catch, watching the chart live: "it seems as if the line is not adapting to the thresholds that were widened." The 7th restyle pass (same day) had moved RSI's dashed lines to `THRESHOLD_LINE_LOW`/`_HIGH` (10/90) to match %B's own pixel height — but real RSI(14) rarely swings below 10 or above 90 (typically 20-80), so on live data the line almost never reached the widened lines, looking like it was ignoring its own thresholds. Presented as a genuine design trade-off (three options: revert to real 30/70, keep 10/90 and rescale the line's own domain, or leave as-is) rather than silently picked — Pieter chose the revert. `RsiOscillator` (`MomentumOscillators.kt`) now draws a local `listOf(30.0, 70.0)` again, not the shared constants; `THRESHOLD_LINE_LOW`/`_HIGH` (`ChartCommon.kt`) reverts to describing only %B's own real band, its original purpose before the 7th pass reused it. RSI's own value line and its Overbought/Oversold/Neutral footer state were unaffected either way — both always read the real 30/70. `LibraryContent.kt`'s RSI & MACD entry and `ATOM_FX_DESIGN.md` §19.4b updated. Verified on-device against live production data (device was briefly on a secure lockscreen mid-session, unlocked before verification completed): EURUSD H4, same RSI=41 reading as the previous audit pass's screenshot — dashed lines now visibly closer to the centreline (30/70, not 10/90) and the line's own Sep 15 dip now clearly pierces the lower dashed line, a real crossing at a real extreme, unlike the previous screenshot where the line never approached the widened lines at all |

---

## C. Outstanding items — prioritized

**To reach a coherent v1 (the app matches the spec's navigation and surfaces):**

1. ~~**3-tab bottom nav + swipe**~~ — ✅ done. `HorizontalPager` nav: `Wheel · Macro · Insights` (Decision 1 resolved). Currency stays in the wheel toggle.
2. ~~**Insights screen**~~ — ✅ done. Aggregates recommendation + breaking headlines + catalyst check + calendar + daily brief + week ahead (Functional Spec §7).
3. ~~**Settings screen + header gear**~~ — ✅ done, since extended with a Notification History screen (2026-09-0x).
4. ~~**Turn on the AI narration (Phase 7 full)**~~ — ✅ done.
5. ~~**Wheel v2 wing rework + Simplification Rework**~~ — ✅ done (2026-09-05/06/09, not tracked in this file until now). 4 wings — Setup (labelled "SETUP" since 2026-09-09, was "OVERALL")/Trend/Momentum/Volatility — hub moved to D1 Regime, pair sheet reduced to 3 tabs, status strip became the ranked-pairs glyph row, the standalone Tradeable Now card retired. See Section B above and `ATOM_FX_DESIGN.md` §17/§19/§20 (synced today).
5b. ~~**Bollinger Band D1 touch alert + Watchlist**~~ (Signals Roadmap §5) — ✅ done (`afd9a25`, 2026-09-09). **Missed in this file's first 2026-09-10 pass** — caught by Pieter, see the correction note at the top of this file. See Section B below for what shipped.
5c. ~~**Regime/Alert Playbook + Reading Window**~~ ("living handbook" — contextual explanations wired to `archetype_change` then extended to 5 more alert types, plus a dedicated full-screen Reading Window destination) — ✅ done (`cb02e06`, `205264e`, `08689aa`). Also not previously tracked in this file — see Section B.
5d. ~~**Hourly recommendation ranking + edge-triggered alert**~~ (Signals Roadmap §5b) — ✅ done, 2026-09-16. See Section A above for what shipped.
6. ~~Push end-to-end verification~~ — ✅ done, 2026-09-10 (see Section B above).

**Polish / optional:**

7. Price-level alerts UI + PAT sync (Functional Spec §9) — still parked (Pieter, 2026-09-03), Settings row present but disabled. **Note (2026-09-17):** when this ships, align its push message to the "{pair} — Level Alert" title convention every other alert type now uses (see the alert-title standardization row above) — it was skipped in that pass since its message lives in `level_ema_alerts.py`, FROZEN, and the feature is dormant. Currently still `"Level Alert — {pair}"` (pair second, not first).
8. ~~`news_themes` tagging~~ — ✅ done.
9. ~~Animation re-verify after wheel v2~~ — ✅ done, on-device (Simplification Rework follow-ups). One open item: `CsmBarStrip` visual polish pass (still plain bars).
10. ~~Bollinger Band touch/reversal alerts~~ (Signals Roadmap §5) — ✅ done, see item 5b above. **This file previously said "not started" — that was wrong, checked and corrected 2026-09-10.**
11. ~~`BreadthSheet.kt` / `SheetTarget.Ring` dead-code cleanup~~ — ✅ already done; this item was stale. **Corrected 2026-09-17** (full-system audit): checked directly, `BreadthSheet.kt` no longer exists anywhere in the repo and `SheetTarget` (`BottomSheetHost.kt`) has no `Ring` case — the cleanup this item asked for already happened at some point, it just never got checked off here.
12. Minor hygiene: no signed release build exists (every install has been a debug APK). **Correction (2026-09-17)**: `versionCode`/`versionName` are NOT still stuck at `1`/`"0.1"` — bumped to `2`/`"0.2"` back on 2026-09-10 (`b7372a3`), confirmed against both `build.gradle.kts` and the actual installed app (`versionCode=2` via `dumpsys package`); this row was stale. `.idea/` *is* already in `.gitignore` — an earlier backlog note claiming otherwise was checked against the repo today and is wrong, don't re-flag it.
13. **EXTEND-layer failure observability gap (found in the 2026-09-17 full-system audit).** `scan_h1.py`'s EXTEND block (`macro_regime`, `recommendation`, `state_alerts`, `bb_touch`, etc.) is wrapped in one broad `try/except` that prints and continues, deliberately — so a bug in additive analytics can never crash the frozen scan (see the block's own comment). But nothing pages on the failure itself: if EXTEND started failing every scan, the only symptom would be stale/missing EXTEND keys in `signals.json`, discoverable only by noticing something's off and checking GitHub Actions logs by hand. Logged as a known, accepted-for-now gap rather than built — proportional to something that's mostly theoretical (no incident of this actually happening), not urgent enough on its own to justify a new self-monitoring push-alert type. Revisit if EXTEND ever does silently break for a real stretch.

**Deferred (post-v1):**

13. Journal (Phase 10+) — parked (Pieter, 2026-09-03).
14. ~~Macro archetype card reframe~~ — raised and deferred 2026-09-05, **resolved 2026-09-10** (see Section B). Insights page rethink is still open, raised the same day, not a defect.
15. **Full-scale website** (raised 2026-09-17, Pieter's own idea) — a browser dashboard consuming the same `signals.json` the app reads, no backend changes at all (a pure new frontend consumer, same as this app). Deliberately **not** a port of the radial wheel — that's a phone-screen-real-estate solution; the website leans into a proper multi-column dashboard grid, real charts/sparklines, sortable tables, and hover interactions instead. A 7-page wireframe (Dashboard, Pair Detail drill-down, Macro, Insights, Watchlist, Alerts & History, Settings) was sketched and approved by Pieter as a direction — Claude Design canvas: https://claude.ai/artifact/LWPWKngAwP9twwQ5YQrPwo. IA mirrors `signals.json`'s own top-level sections (Dashboard's Setup Ranking table is the wheel's replacement as the hero "at a glance" element); built from the app's real dark-theme tokens (`Color.kt`) and Inter (`Type.kt`), not a generic template. One real gap flagged for whenever this is built for real: `signals.json` is a single latest snapshot, not a time series — the git history of `data/signals.json` itself (committed every scan) is a free, ready-made archive for real historical charts without touching the frozen scanner. Post-v1, no timeline yet.

16. **BB touch alert rework — Pieter's own idea, to be discussed (raised 2026-09-18).** Flagged
    while completing the glance panel: "The pair 12 period %B should not be discarded, maybe just
    deferred for now, I have an idea for the BB touch alerts I want to discuss later." Nothing was
    designed or built — this is a placeholder so the thread isn't lost. What's parked alongside it:
    the 12-period pair %B chart (`ui/sheets/PercentBChart.kt`, intact but currently unused —
    **do not delete as dead code**) is expected to come back as part of whatever this becomes.
    Related and also still shelved: the BB "Exhaustion Score" composite (designed then shelved
    2026-09-17 over Pieter's ADX objection) — if that comes back, start from "what replaces ADX",
    not from a blank slate.
17. ~~**Configure the Apps Script trigger for `scan_m15.yml`**~~ — ✅ done (Pieter, 2026-09-18,
    same day). A second `everyMinutes(15)` trigger, self-pacing to ~45 min via a stored
    timestamp, weekday-gated, added alongside the existing hourly `runScheduler` trigger.
    Confirmed live via a real `chore: m15 scan` commit from `github-actions[bot]` in production
    — which is also what surfaced the `attach_momentum_series`/`attach_bollinger_series` bug
    documented in Section B's M15 row above (found and fixed same day, before it could cause
    lasting data loss).

18. **Market-closed weekend bars contaminate the D1/H4 series on `main` (found and measured
    2026-09-19; Pieter confirmed it should be followed up; **FIXED 2026-09-19 with his explicit sign-off** — see "Fix" below).** Twelvedata began
    emitting market-closed weekend bars on 2026-01-11 (a flat-ish Sunday pre-open block and a
    recurring Saturday block — recorded as `DECISION-007` in the `research` branch's
    `scanner/extend/agg_nyclose.py`, whose `_within_fx_week` drops bars outside
    [Sunday 17:00 ET, Friday 17:00 ET)). That filter is **not on `main`**: `bb_touch.py::_d1_ny_close`
    (which also feeds `bollinger_series` and the D1 `momentum_series`) groups every H1 bar by
    `(NY time − 17h).date` with no closed-market filter, and the frozen aggregator's H4 has none either.
    Labels are the session's OPEN date, so real sessions read Sun–Thu and the closed-only sessions read
    Fri/Sat. **Measured** on the same real H1 history, main's own functions, live vs filtered, since
    2026-01-12: **28%** of D1 label-days are closed-market-only bars (70 of 250 per pair), with a median
    range about **half** a real session's (EURUSD 25.9 vs 50.9 pips, USDJPY 25.5 vs 82.0); the BB touch
    (12-period — the **alert** path) state differs on **17.3%** of comparable days (373 of 2,160) and the
    daily-label sequence shows **409 vs 270** none→touch transitions across 12 pairs (about **+50%**
    more, at the daily-label level — not a count of pushes actually sent); the latest values the app shows
    differ by a mean **2.3 RSI points** (max 4.5) and **6.4 %B(20) points** (max 14.7), enough to flip some
    "Stretched" footers (e.g. USDCAD %B 89.9 → 104.6, USDCHF 88.3 → 99.5). Only windows after 2026-01-11
    are affected. **Fix is a separate, Rule-#1-style decision** (it moves numbers an alert fires on):
    port the closed-market filter to `main` as an EXTEND change to the shared D1/H4 bar builders, then
    re-verify `bb_touch`, `bollinger_series`, `momentum_series`, and anything else reading them. The
    Crowd score (Roadmap §5c) is built on filtered bars and does not depend on this fix.

    **Frozen-number impact, measured 2026-09-19** (read-only; the frozen `aggregator.build_tfs` /
    `pills.classify_full` / `score.score_pair` run on the SAME 5,000-row H1 window of real data, once as live and
    once with the closed-market rows dropped first; 12 pairs): the **H1 pill differs on 7 of 12 pairs, the H4 pill on
    5 of 12, the H4 score on 11 of 12 (mean 1.7 points, max 5.0), and H4 ADX by a mean 3.7 points (max 12.7)** — e.g.
    USDCHF H4 pill neutral → bull, USDCAD H4 ADX 44 → 53. So the contamination reaches the frozen wheel, pills and
    regime inputs, not just the EXTEND chart series and the BB touch alert. **A fix therefore has two scopes:**
    (A) EXTEND-only — filter inside `bb_touch._d1_ny_close` and the shared D1/H4 series builders; fixes the charts and
    the BB touch alert, leaves the frozen numbers as they are; (B) filter the raw H1 in `scan_h1.py` before anything is
    built — fixes everything, but **changes frozen numbers (FROZEN-TOUCH: needs Pieter's explicit sign-off, per
    Rule #1)**. **Option B has a hard side effect:** the frozen D1 scorer needs ≥ 210 bars and the live window gives only
    ~209-211 UTC-day bars *because the phantom weekend days count toward it*; dropping them leaves ~179, so the frozen D1
    pill, `regime_d1` and every D1-dependent number would go dark unless the H1 fetch is deepened (a second Twelvedata
    call per pair per scan, roughly doubling `scan_h1`'s credit use, 144 → 288 a day against the 800/day budget that
    `scan_m15` already shares). Not done; a decision, not a chore.

    **Fix (2026-09-19; Pieter: "you can do all of these ... including the full B fix that changes frozen numbers").** Option B,
    implemented without a second API call and without editing any frozen calculation file: new `scanner/extend/fx_week.py` drops
    closed-market rows and merges each wheel pair's fetch with a persistent H1 history (`data/h1_history/<PAIR>.csv`, 12 files,
    4.2 MB, seeded once from the deep cache, extended by every scan and trimmed to 6,000 real rows); `scan_h1.py`'s fetch loop hands
    the pipeline `prepare_h1(...)` (a guarded import — a broken module falls back to the fetched rows); `scan_m15.py` drops the same
    rows from its M15 frames; `crowd_data.py` now imports the one shared rule. **Verified on real data:** with the store every pair
    has ~247 D1 bars (the frozen scorer needs 210) and ~1,291 H4 bars, and every pair scores on both; a full offline `scan_h1`
    run on real windows completes with no errors, the Crowd score's D1 series grows from 50 to 90 points, and the numbers move as
    measured (H1 pill 7/12 pairs, H4 pill 5/12, BB touch state 4/12; regime_h4 Risk-On → Mixed and regime_h1 Ranging → Risk-Off on
    that snapshot). **One-scan migration guard:** `signals.json` carries `bars_convention: "fx_week_v1"`; the first scan under it
    suppresses the state-transition pushes, the recommendation alerts, the Gold Signal PUSH (state still saved, the 2026-09-17
    edge-trigger precedent) and `scan_cot.py`'s one Conviction comparison, so the step-change cannot fire a burst of spurious
    notifications (verified: zero pushes on the first and second scan). Price-level alerts are untouched. Paper trail:
    `scanner/FROZEN.md`, `ARCHITECTURE.md` §2. Tests: `tests/test_fx_week.py` (13). **Follow-ups still open:** the sibling series'
    labels (open date vs the Crowd score's trading day) are unchanged; the app's Library text describing D1 conventions was not
    re-audited for this change.

    **REOPENED 2026-09-19, same day — the fix above is DISABLED (kill switch, `_FX_WEEK_ENABLED = False` in `scan_h1.py` and
    `scan_m15.py`), before any hourly scan ran with it. Root cause found while comparing against TradingView: Twelvedata's hourly
    forex timestamps are NOT UTC. They are Australia/Sydney local time — UTC+10 in AEST (Apr-Oct), UTC+11 in AEDT (Oct-Apr).**
    Evidence: five TradingView (OANDA) candles across 2020-2026 and three pairs each match our raw hourly rows at exactly that
    offset (AUDUSD 4H 10 Jun 2026: 0.8 pips at +10 h vs 25 pips unshifted; GBPUSD and CADJPY last 4H bars of 18 Sep 2026: 2.1 / 6.8 pips at
    +10 h vs 109 / 129 unshifted; AUDUSD D1 11 Mar 2020: 3.1 pips at +11 h vs 27 at +10 h). `fx_week.py`'s week rule is written in New
    York/UTC time, so on the raw labels it would have DROPPED real Friday trading (label Fri 21:00 - Sat 07:00 = real Fri 11:00-21:00 UTC) and KEPT
    weekend quotes (label Sun 21:00 - Mon 07:00 = real Sun 11:00-21:00). Bad premise, not a bad idea: the fix needs the timestamps converted to true UTC
    FIRST. **What else this affects (all pre-existing, not caused by the fix):** the frozen aggregator's "UTC" D1 is really a Sydney-midnight day
    (closes 14:00 UTC / 10:00 New York) and its "UTC" H4 blocks are 10-11 h away from real UTC blocks; `crowd_data.py`'s "17:00 New York close" D1 and
    "NY-session" H4 bars are therefore built at the wrong times (H4 by 2 h, D1 by ~10 h), so the Crowd score card is not aligned to TradingView; the
    Library text claiming a 17:00-NY D1 for %B / BandWidth / RSI-MACD / Crowd score is not true of the shipped numbers; the research stores and Experiments
    2-7 used the same labels (see `RESEARCH_LOG.md`). **Not yet decided / needs Pieter's sign-off:** convert every hourly frame to true UTC at fetch (one
    tested function, `Australia/Sydney` -> UTC via zoneinfo), THEN apply the real-week rule and rebuild the history store. That changes every D1/H4 number
    once more (a frozen-INPUT change of the same kind as 2026-09-17/19's), so it will not ship without his explicit go-ahead and a migration guard.

    **FIXED 2026-09-19 (second attempt; Pieter: "Yes, you can fix it").** `scanner/extend/fx_week.py` now converts every fetched hourly frame from Sydney time to
    true UTC FIRST (`to_utc_labels` / `normalize_frame` / `prepare_h1(source_tz="Australia/Sydney")`, DST-aware via zoneinfo), THEN drops closed-market rows; applied at the
    `scan_h1` and `scan_m15` call-sites only (no frozen file touched); `_FX_WEEK_ENABLED = True` again; `BARS_CONVENTION = "fx_week_v2"` so the first scan is a migration scan
    (no pushes, no Conviction alerts); `data/h1_history` rebuilt from the raw cache in UTC (GBPUSD last close 1.33941 vs TradingView 1.33947 — it was 1.33555 before). 149 tests pass,
    Rule #1 green, offline end-to-end scan on real windows: no errors, no pushes on the first or second scan, crowd D1 series 90 points. **Validated against TradingView Data Window readings
    Pieter took (AUDUSD 1D 26 and 23 Jan 2026, GBPUSD 1D 24 Sep 2024, AUDUSD 4H 10 Oct 2025 13:00 UTC, CADJPY 4H 1 Apr and 30 Jul 2026): candle prices within 0.1-3 pips; the Crowd score
    matches EXACTLY on 5 of 6 bars (62/62, 37/37, 62/62, 15/15, 25/25); the sixth (AUDUSD 4H) is 72 vs TradingView's 62 — one 10-point condition (climax) sits on its threshold and TradingView's
    H4 COT value is missing there (Ø): TradingView computes the COT percentile over 4,680 H4 bars, more history than a free plan loads, so TradingView H4 scores lack the 25-point COT factor on older
    bars while ours include it. COT percentile matches to within 0.2 on all D1/H4 bars that have one (90.385 vs 90.385).** Factor values: %B +-0.02, z +-0.06 (H4 +-0.22), stretch +-0.08, RSI +-0.4 (CADJPY
    4H +1.06). **Open:** (a) the research port's ADX differs from TradingView's on some bars (AUDUSD D1 26 Jan: ours 41.9 vs 35.8; GBPUSD and CADJPY within 0.7) — irrelevant to the score, but
    Experiments 6-7 used it for the strong-trend shading, so their "background" was not exactly TradingView's; (b) Experiments 2-7 must be RE-RUN on converted bars (RESEARCH_LOG caveat);
    (c) the first production hourly scan under `fx_week_v2` still has to be checked (marker, no push burst, no errors, `crowd_series.d1` ~90 points, `data/h1_history` committed).

    **UNIFORM CLOCK (2026-09-19, same evening; Pieter: "it is better to use a uniform dataset ... we can align the frozen engine if you deem it correct").** The frozen engine's D1/H4 are now
    aggregated on the NEW YORK trading clock — D1 = the 17:00 New York close (Sunday session rolls into Monday), H4 blocks at 17/21/01/05/09/13 New York (TradingView's) — by handing the
    frozen aggregator H1 rows re-labelled as New York wall time + 7 h (`fx_week.to_trading_clock`, one call-site in `scan_h1.py`; H1 keeps UTC labels; NO frozen file edited).
    It also removes a real defect the UTC clock still had after the timestamp fix: a 2-3 hour Sunday stub D1 candle and 1-hour/3-hour stub H4 blocks every week feeding ATR/ADX/EMA
    (test: two June weeks give exactly 10 D1 and 60 H4 candles on the trading clock). `BARS_CONVENTION` -> `fx_week_v3`; folded into the SAME first migration scan (no hourly scan had run yet), so
    there is still only one migration. Offline E2E on real windows: no errors, no pushes on the first or second scan; old-vs-new pill differences D1 7/12, H4 3/12, H1 3/12; `regime_d1` Ranging -> Risk-Off,
    `regime_h4` unchanged. 151 tests pass, Rule #1 green. The Library's "H4 and H1 use UTC boundaries" was corrected. **Now every D1/H4 number in the app (frozen pills, ADX, regime, CSM, the Crowd score,
    %B/BandWidth/RSI/MACD) is on the same 17:00-New-York / TradingView bar grid.** Still not TradingView-identical: the price vendor (Twelvedata vs OANDA), the macro/news feeds, and any frozen
    indicator that has no TradingView equivalent.

    **FIRST LIVE SCAN VERIFIED 2026-09-19 14:14 UTC** (manual "Hourly Scan" run on `7b8c9f2`, green): `bars_convention: fx_week_v3`, schema 12, `crowd_series` d1 = 90 and h4 = 90 points for all 12 pairs,
    latest bars dated 2026-09-18 with COT resolved, `last_alert` unchanged (no push burst), commit touched only `data/signals.json`; `data/h1_history` needed no rewrite (the live fetch, converted to UTC, equals the
    rebuilt store row for row — an independent confirmation of the conversion). Live regimes: D1 Risk-Off, H4 Risk-Off, H1 Ranging. Remaining open: Experiments 2-7 re-run on converted bars; research-port ADX vs TradingView.

    **REGRESSION FOUND AND FIXED 2026-09-19 (Pieter: "On H4 the graphs do not show dates").** The uniform-clock change moved the frozen H4 frame to the New York clock, but `tf_dates.h4_dates()` (the label recovery behind the
    %B / BandWidth / RSI / MACD 4-hour charts) still floored on UTC blocks; the row counts stopped matching and `_series_for` dropped the dates, so the live `bollinger_series.h4.dates` and `momentum_series.h4.dates` were EMPTY after the
    first `fx_week_v3` scan. Fix: `h4_dates(raw, trading_clock=True)` applies the same New York-clock shift, chosen by `df.attrs["clock"] == "ny"` set in `scan_h1.py`; regression test added (60 dates = 60 frozen H4 rows over two weeks,
    weekdays only). Offline scan: 90 H4 dates per pair again. The chart values were unaffected (only their labels). **Lesson:** my offline check compared scores and pills but not every EXTEND series' dates; the uniform-clock
    change should have been searched for every consumer of the H4 frame's labels.

    **Long-press chart changes (2026-09-20, Pieter's ask), built and checked on a physical device:** (1) every long-press chart now shows **six** date/time labels under the plot (`ChartCommon.drawDateRow`, evenly spaced, first and last bar included; a run of
    bars on one calendar day draws that label once — mostly H1); (2) the Crowd chart draws dashed **20, 40 and 60** lines with tiny value labels (60 stays the flag line); (3) the Crowd chart shades the **strong-trend regime with REVERSED colours**
    (strong uptrend = red, strong downtrend = green; 10% opacity). (3) needed a backend field: `crowd_series.<d1|h4>.regime` (+1/0/-1 per bar, `crowd_score.regime_states`, the Pine's own ADX/EMA200/3-bar rule), **schema v13**. Validated against the six
    TradingView readings: 5 of 6 regime states equal TradingView's (AUDUSD D1 23 Jan 2026 shades one bar earlier in ours: ADX is higher on our feed). 158 Python tests, 35 Kotlin tests (only the 2 known WheelMapperTest failures). **Shading will show after the next
    hourly scan writes the field**; until then the app draws none (older `signals.json` tolerated). Judgment calls: the value labels on the reference lines; de-duplicating repeated dates on H1; 10% shading opacity.

    **Date labels reworked 2026-09-20 (Pieter: "stagger the dates evenly, 3 days apart ... the exact amount is not that important, but they have to be evenly spaced").** The six-labels-by-bar-index version read uneven in calendar terms (Aug 31, Sep 3, Sep 8, Sep 10 ...).
    `ChartCommon.dateTicks` now labels CALENDAR-even dates counting back from the newest bar (smallest step of 1/2/3/4/5/7/10/14/21 ... days that keeps <= 7 labels and no crowding); 3 days first collided (a Saturday date sits at the Monday open beside Tuesday's label),
    so the 4-hour chart now shows 4-day steps (Sep 2, 6, 10, 14, 18). Because weekends have no bars, spacing on screen is slightly narrower across a weekend while the dates stay exactly one step apart. Device-checked; 37 Kotlin tests (2 known failures).
    A **demo data file** (`demo/regime_demo_signals.json`: live data + real regime for every pair + synthetic showcase patterns on EURUSD/GBPUSD) lets the shading be viewed before the next scan writes the real field: paste its raw URL into Settings > signals.json URL, clear it to return to live.

19. **Unify the in-app Library (and the Playbooks) with `ATOM_FX_LIBRARY_STYLE.md` — DONE 2026-09-19 for the
    Library; Playbooks audited (raised by Pieter, 2026-09-19).** A scripted audit of all 35 `LibraryContent.kt`
    entries against the guide (length ceilings, dates and names, em-dash stacking, list formatting) found **24
    with issues**; 23 were rewritten (facts kept, narration and dates cut, list-shaped content put on lines,
    Home/alert behaviour moved from `whyItMatters` into `howItWorks`), including the dated %B, BandWidth, RSI/MACD,
    Currency %B and State-Transition entries. **After the pass: 0 dates, names or history in any displayed
    string; 10 entries still run a few words over a soft ceiling** (`continuation` and `conviction` are
    irreducible lists of weights and inputs; `crowd-score` is a list of eight conditions; the rest are 1–5
    words over). Two content corrections rode along: the Regime entry said "Four votes" but lists three plus an
    override, and the State-Transition entry named the H4 regime and "five" alerts where the alert reads the D1
    regime and lists four (it now points at the separate BB touch, Recommendation, Crowd score, Positioning and
    Gold Signal entries). Checked in the app on a physical device (list lines render as separate lines). **Playbooks:**
    the same scan of `AlertPlaybookContent.kt`, `RegimePlaybookContent.kt` and `TechnicalRegimePlaybookContent.kt`
    found **no dates, names or history in any displayed string** (the dated text is in code comments). Ten `coreStory` /
    `mechanism` / `relationToOtherSystems` fields run 90–123 words, but the guide sets no ceiling for a Playbook's
    story field (only for `howItWorks`'s equivalent), so they were left as they are. **Still open:** a
    human read of the rewritten entries for tone (the checklist's "would a first-time reader understand this" test
    is not scriptable).

20. **Twelve Data's terms vs raw price data in PUBLIC repos (raised by Pieter 2026-09-20; forward fix DONE, history clean-up needs his decision).** Read twelvedata.com/terms (last updated 2026-01-01): **2.2(c)** permits
    derived data that "cannot be reverse-engineered to recreate the original Data" (signals, scores, indicator series, experiment results are fine); **2.2(e) / 2.3(b)** bar redistributing or externally displaying the data without a
    Redistribution Rights add-on or written agreement; **2.3(g)** bars storing/caching beyond the Documentation's permitted timeframes; **2.3(l)** bars commercial use of Free-tier data; the pricing page calls the Basic plan "internal non-display usage".
    (Summarised from the fetched page — read the source; this is not legal advice.) **What was public:** (a) `data/h1_history/` on `main` (12 files x 6,000 raw hourly candles, committed by the scans since 2026-09-19 ~13:50 UTC); (b) on the `research`
    branch (pushed earlier) the price stores `data/{d1_utc_daily,d1_nyclose,d1_nyclose_long,h4_ny_long,h4_utc_long}` (~64 aggregated OHLC files). **Done (2026-09-20, `main`):** `data/h1_history/` is now git-ignored and untracked; the scan workflow keeps it in a PRIVATE
    `actions/cache` (per-run key + prefix restore-key); on a cold start (evicted/empty cache) `scan_h1.py` backfills once via `scanner/extend/h1_backfill.py` (12 extra API calls, tested: store rebuilt to 6,000 rows/pair, D1 pills and 90 crowd points intact); rebuild
    locally with `tools/seed_h1_history.py`. **PURGE DONE 2026-09-20 (Pieter approved, both branches): the six price paths were removed from every commit of `main`, `research` and the archive tag (git filter-repo on a mirror clone, lease-guarded force-push), verified zero blobs left; every commit hash changed (dates/messages preserved; old->new map in `docs/HISTORY_REWRITE_2026-09-20.md`). GitHub may still serve old SHAs until it garbage-collects: ask GitHub Support to purge cached views if you want them gone too.** (Original problem statement: the OLD commits still contain those files on GitHub (main since 2026-09-19; research since earlier). Removing them means rewriting history and force-pushing (`git filter-repo`), which changes commit
    hashes (the research log cites some), briefly disturbs the scan bot's pushes, and GitHub may still serve old SHAs until it garbage-collects (support can purge). **If a public website/app is ever built:** displaying data to others needs the right plan/add-on; ask Twelve Data.


---

## D. Open decisions (need your call before building)

1. **Currency tab vs. the wheel toggle. — RESOLVED (Pieter, 2026-09-02): 3-tab nav
   `Wheel · Macro · Insights`.** Currency strength lives inside the wheel's Currencies/Pairs
   toggle (wheel v2); there is no separate Currency tab.
2. **AI cadence & spend. — RESOLVED (Pieter, 2026-09-03): gold signal, regime-bias flip, or
   12h — whichever comes first**, not a flat timer.
3. **Settings theme override — RESOLVED**: built as specced, `system · dark · light` (stored).
4. **Wheel wing consensus timeframes — RESOLVED (Pieter, 2026-09-06, reaffirmed 2026-09-09):
   fixed, never a D1/H4/H1 toggle.** Tried wiring the wheel to the existing D1/H4/H1 toggle twice
   (2026-09-06 and implicitly reconsidered 2026-09-09) and reverted both times. Settled: D1 Regime
   → H4 Trend → H4 Momentum → D1 Volatility. If a future session is asked to make any wheel
   element "follow the toggle" again, check `[[atom_fx_simplification_rework]]` first.

---

## E. Recommended next build order (hand to Claude Code, one at a time)

Each step must read `CLAUDE.md` first, then the cited spec section, and ship building before the next.

1. ~~**Nav shell**~~ — ✅ done.
2. ~~**Insights screen**~~ — ✅ done.
3. ~~**Settings screen + header gear + theme override**~~ — ✅ done.
4. ~~**AI narration on**~~ — ✅ done.
5. ~~**Wheel v2 + Simplification Rework**~~ — ✅ done.
6. ~~Push end-to-end test~~ — ✅ done.
7. Price-level alerts UI (§9), if wanted — next up.
8. Journal when you're ready for post-v1.

> Keep this file current: when Claude Code finishes an item, it should flip the Status here in the
> same commit. That one habit prevents the "what's left?" confusion from recurring.
