# ATOM FX — EXTEND layer (additive analytics).
#
# Every module here READS frozen outputs and produces NEW signals.json keys.
# None of them import-and-modify a frozen module. See docs/ATOM_FX_ARCHITECTURE.md §5.
