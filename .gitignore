.venv/
__pycache__/
*.pyc

# Local H1 bar cache (per-pair OHLCV pulled for on-device debugging/verification) — not
# something the scan bot commits, and not needed to reproduce a scan (data/signals.json is
# the committed artifact).
data/h1_cache/

# Android / Gradle
local.properties
.gradle/
build/
app/build/
*.iml
.cxx/
captures/
.externalNativeBuild/
.idea/

# Persistent hourly price store (scanner/extend/fx_week.py): raw Twelve Data candles must NOT be published in this public repo
# (twelvedata.com/terms 2.2/2.3). It lives in the scan workflow's private Actions cache; rebuild locally with tools/seed_h1_history.py.
data/h1_history/
